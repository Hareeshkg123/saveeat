/**
 * types
 */
export interface FoodType {
  name: string;
  description: string;
  rating: string;
  location: string;
  price: number;
  discountPercentage: number;
}

export interface ReservationCenterType {
  name: string;
  description: string;
  rating: string;
  location: string;
  price: string;
  discountPercentage: string;
}
